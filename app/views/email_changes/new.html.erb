<% @page_title = "メールアドレス変更" %>

<div class="container mt-5">
  <div class="row">
    <div class="col-11 col-md-6 mx-auto">
    <%= render 'shared/error_messages', object: @user %>

        <p>新しいメールアドレスを入力して変更ボタンを押してください。</p>
        <p>新しいメールアドレスに確認メールが送られますので、メール本文のURLをクリックして変更を完了してください。</p>
        <p style="color: red; font-size: 0.80em;">このページの変更ボタンを押しただけでは変更が完了しませんので注意してください。</p>

        <div class="border col-12 rounded p-5 mx-auto">

        <%= form_with url: email_changes_path, local: true do |f| %>
          <div class="mb-3">
            <%= f.label :new_email, "新しいメールアドレス", class: "form-label" %>
            <%= f.email_field :new_email, class: "form-control" %>
          </div>

            <%= f.submit "変更", class: 'btn btn-custom w-100' %>
          <% end %>

        </div>
    </div>
  </div>
</div>

